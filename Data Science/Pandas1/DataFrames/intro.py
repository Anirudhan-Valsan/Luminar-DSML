#Data Frames are used for 2d data

"""
head
tail
isna
read_csv
describe
dtype
shape
iloc
fillna
loc  newdf = df.loc[df['colname']conditiomn]
\ is used to extend the query to the next line
sort_values(column_name,axis)
drop
drop_duplicates
count()
max() - newdf = olddf.groupby('colname')['colname 2'].max()
min()
mean()
sum()
#joining operation
#inner join --->  new_df = pd.merge(df1,df2,on='id',how='inner')
newdf = olddf.groupby('colname)['colname'].count()
left outer join #it returns all the row from left dataframe + and the data matching common field from the other
dropna
"""


#print("><"*100)